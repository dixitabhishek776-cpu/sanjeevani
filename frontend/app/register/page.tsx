"use client";
import { FormEvent, useState } from "react";
import { register } from "../../lib/api";
export default function RegisterPage(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [name,setName]=useState("");const [message,setMessage]=useState("");const [error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");try{await register(email,password,name);setMessage("Account created. Check your email for verification instructions.");}catch(err){setError(err instanceof Error?err.message:"Registration failed");}}return <main className="auth-shell"><form className="auth-card" onSubmit={submit}><h1>Create your Sanjeevani account</h1><p>Your wellbeing data should remain under your control.</p><label>Name<input value={name} onChange={e=>setName(e.target.value)} maxLength={100}/></label><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input required type="password" minLength={12} value={password} onChange={e=>setPassword(e.target.value)}/></label><small>Use at least 12 characters. Avoid using a password you use elsewhere.</small>{error&&<div role="alert" className="error">{error}</div>}{message&&<div role="status" className="success">{message}</div>}<button>Create account</button><a href="/login">Already have an account? Sign in</a></form></main>}
